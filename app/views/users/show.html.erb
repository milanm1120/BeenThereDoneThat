<!-- <h1> Users#show</h1> -->

<h1>Hello <%= current_user.first_name.capitalize %>! Here are all your travel pins!</h1>

<% if current_user.pins.empty? %>
    <h3> You Don't Have Any Pins Yet! </h3>
<% else %>
    <h3>--------------------------------------</h3>
        <% current_user.pins.each do |pin| %>
            <%= link_to pin.destination.city_country, pin_path(pin), class: "font_color" %><br><br>
        <% end %>
    <h3>--------------------------------------</h3>
<% end %>
