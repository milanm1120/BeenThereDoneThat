Edit <%= @pin.destination.city %>

<%= render partial: "layouts/error", locals: { object: @pin} %>

<%= form_for([@destination, @pin]) do |f| %>

    Date Visited: <%= f.date_field :date %></br>
    Rating: <%= f.number_field :rating, min: 1, max: 5 %></br>
    </br>

    <%= f.submit "Update Pin" %>
<% end %>

