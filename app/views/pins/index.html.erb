<h1>Pins#Index</h1>


<% if @pins.length == 0 %>
    No Pins Yet
<% else %>
    <% @pins.each do |p| %>
        Rating: <%= p.rating %></br>
        Date: <%= p.date %></br>
        Destination: <%= p.destination.city %>, <%= p.destination.country %></br></br>
    <% end %></br>
<% end %>
